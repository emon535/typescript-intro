class Point {
  x: number;
  y: number;
  doDraw() {
    //DrawFunction
    console.log("Cordinates are X:", this.x, "and Y is :", this.y);
  }

  getDistance2() {
    //getDistance
  }
}

let point: Point = new Point();
point.doDraw();
point.getDistance2;
