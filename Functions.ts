let mylog = function(message) {
  console.log(message);
};

let doLog = message => console.log(message);
let doLogwithEmpty = () => console.log(message);

let drawPoint = (point: { x: number; y: number }) => {
  //draw
};

drawPoint({
  x: 1,
  y: 23
});
